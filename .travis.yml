# travis-ci 
os: linux
dist: xenial

language: node_js
node_js:
#- '10'
- '12'

branches:
  only:
  - master
  
git:
  quiet: true

install:
- npm install

script:
- npm run-script react-scripts build
